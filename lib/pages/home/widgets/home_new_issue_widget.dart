import 'package:flutter/material.dart';

class HomeNewIssueWidget extends StatelessWidget {
  const HomeNewIssueWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('New Issue'),
    );
  }
}
